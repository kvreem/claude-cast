{
  "mcpServers": {
    "claude-cast": {
      "command": "bun",
      "args": ["${CLAUDE_PLUGIN_ROOT}/packages/stream-server/server.ts"],
      "env": {
        "CLAUDE_CAST_ROOT": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
