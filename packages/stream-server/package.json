{
  "name": "@claude-cast/stream-server",
  "version": "0.1.0",
  "private": true,
  "main": "server.ts",
  "scripts": {
    "start": "bun install --no-summary && bun server.ts",
    "dev": "bun --watch server.ts",
    "typecheck": "bun x tsc --noEmit"
  },
  "dependencies": {
    "@claude-cast/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "node-mpv": "^1.5.0"
  }
}
